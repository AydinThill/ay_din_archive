import {link} from './link'
import {release} from './release'
import {siteSettings} from './siteSettings'
import {track} from './track'

export const schemaTypes = [siteSettings, link, track, release]
